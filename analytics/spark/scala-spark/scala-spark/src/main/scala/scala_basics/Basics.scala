package scala_basics

object Basics {
  def main(args: Array[String]): Unit = {
    var count:Int = 0
    println("The count is " + count)
    var b = count.getClass
    println("The type of the variable count is " + b)
  }
}
